# M10P 雷达升级详细修改计划（待敲定）

日期：2026-09-24。目标主控：**STM32F407ZGT6**。目标雷达：本目录手册对应的 **M10P-20K 串口版，512000 bps**。

本轮交付为修改计划、资料核对及当前工程构建证据；**未修改固件源码、未生成已移植 M10P 的固件、未烧录或进行实车验证**。按本次用户要求，先审定本计划，再实施。主工程选用 `2-LXY传承/LXY传承-副本`；原版、谢露工程只作参考。

用户已指定 0° 是车辆正前方、主控是 F407ZGT6，不再把这两项作为型号选择问题。附带手册、旧方案、对话记忆均作为资料，里面的操作建议、旧决策和“已完成”字样不代替本次请求或当前源码证据。

## 1. 推荐敲定的总体方案

1. 保留标准外设库、168 MHz 主频和现有车控外设；USART2 PA3 接收，DMA1 Stream5 Channel4。M10P 模式禁用 PA2 的 LD14P 电机 PWM。
2. 用 **1024 B 循环 DMA + 8192 B 软件接收队列**替代 1798 B Normal DMA 停机重装流程。总接收缓存增大，但 DMA 本体缩小以降低交付延迟，不能简单把 DMA 扩成一整圈。
3. 新建独立 M10P 流式解析器，严格 160 B、大端、70 距离槽、FFFF、bit15 高反及 15° 插值；保留完整原始点，不照搬例程的半包平均。
4. 以车后方向为圈切点，按角度展开组圈，采用 **2 × 2048 点**紧凑原始缓存；丢包、无回波、超容量、陈旧扫描分别记录。
5. 统一原生角与算法角：**原生 0°前、90°右、180°后、270°左；算法继续 x 向右、y 向前，θ=wrap(90°−α)**。在适配层转换一次，不把下游所有 90° 改成 0°。
6. 建立 720 格、0.5°角度索引；算法工作容量暂保留 800，侧边界保留 400、前方数组保留 200。原始高密度点参与筛选、近障和置信度计算，不把所有浮点数组一并扩成 2048。
7. 保留当前 PD 时间归一化、模式切换抑制、TurnGuard 及启动零输出。在此基础上补齐输入年龄、覆盖率和速度许可；再分阶段升级边界、断点、障碍和速度规划。
8. 每阶段有独立回归与回退点。首个可运行版本先证明“协议正确、坐标正确、连续接收、故障可停”，再启用增强算法；最终交付可包含两种算法配置供对照。

## 2. 资料依据与当前事实

### 2.1 资料优先级

| 资料 | 本次核对内容 | 使用方式 |
|---|---|---|
| `m10p/M10P_20K_数据输出格式.pdf`，V2.0，2022-04-07，PDF第3～4页 | 包结构、串口参数、FFFF、高反、插值公式 | 本次解析主依据，已逐页渲染核对表格 |
| `m10p/M10P_20KHz_用户手册.pdf`，V1.0.0 | PDF第8页参数，第11/13/14页接口电气，第17页坐标，第24页启动描述 | 重新提取29页，核对坐标原图；原生顺时针为正、出线端对面为0° |
| `m10p/STM32读取M10P雷达数据说明.pdf`，7页 | 第3～5页协议与坐标，第6～7页例程接线和抓图解释 | 辅助验证，不把示例 UART5 引脚当作本车接线 |
| 用户附图与本次文字 | 0°正前方、STM32F407ZGT6 | 本次安装与平台约束 |
| `雷达升级评审/01～03`、`雷达升级修改方案.md`、`激光雷达参数汇总.md` | 旧实施路线、资源计算、风险、参数矛盾 | 采纳仍适用的内容；过时结论在下表修正 |
| 当前主工程源码与本次隔离构建 | 驱动、控制、遥测、内存和项目设置 | 判断实际实现状态的依据 |

原始手册在标题、正文中多次写 M10，但本次按所附 20K 串口协议识别，**不混入 M10-10K 的92 B格式或 M10P-PHY 网络协议**。STM32说明中“Byte_6、Byte_6”等笔误以协议表的 Byte_6/7 为准；“一个字节代表一个距离”的截图说明不能推翻距离占两字节的表格。

芯片官方核对：[ST STM32F407ZG](https://www.st.com/en/microcontrollers-microprocessors/stm32f407zg.html)、[DS8626 数据手册](https://www.st.com.cn/resource/en/datasheet/stm32f407zg.pdf)、[RM0090](https://www.st.com.cn/resource/en/reference_manual/rm0090-stm32f405415-stm32f407417-stm32f427437-and-stm32f429439-advanced-armbased-32bit-mcus-stmicroelectronics.pdf)。ZG为1 MiB Flash；192 KiB系统RAM中包含独立64 KiB CCM，普通SRAM按128 KiB预算；DMA1 Stream5/Channel4对应USART2_RX，DMA缓存不得放CCM。本次只核对相关硬件事实，不声称逐页审读完整参考手册。

### 2.2 旧评审与当前源码的差异

| 项目 | 当前事实 | 本次处理 |
|---|---|---|
| 雷达身份 | main 注释写M10系列，实际230400、47 B、0x54、LD14P解析 | 修正注释、配置和实际实现，不能仅改名称 |
| LD14P接收解析 | 已跨块拼包、清旧点、返回点数；`Diag_RadarPacket` 已校验CRC/VerLen | 不再列为完全未修复；M10P另建解析，不能调用LD14P校验 |
| DMA共享 | `DMA_RX_DONE` 已volatile，main在关中断区复制1798 B快照 | 所有权已有缓解，但停DMA和长拷贝仍须替换 |
| 上电电机与舵机 | 电机初值0，舵机初始化用`SERVO_PWM_MID` | 保留，不重复套旧“90%起步/中位除10错误”修复 |
| 超时保护 | TIM5每10 ms计龄，50 ticks锁停，复位前不自动恢复 | 保留锁停原则，新增更快的感知许可层与真实观测年龄 |
| PD | 已按`115000/dt_us`归一化D，切模式重建历史 | 禁止再除一次dt；保留旧参数尺度，完善输入时间契约 |
| 弯道保持 | 已有350 ms TurnGuard与两帧出弯确认 | 保留有界保持，审查帧数随12 Hz变化的问题 |
| 遥测 | `ble_diag/ble_tx`、TIM6时钟、诊断V2已存在 | 扩展现有协议与PC解码器，不重建第二套遥测 |
| 有效点过滤 | main仍给HANDLE3_2传800；函数已不掐头尾10点，但注释仍说会掐 | 传真实数量，纠正注释，禁止按缓存头尾删点 |
| 内存 | 本次RW+ZI为48,616 B，不是旧58,216 B | 用本次构建作预算基线 |
| TIM6 | 已被诊断时钟占用 | 复用已有时基，不再次初始化成另一个用途 |
| 芯片工程配置 | uvproj仍为VETx，Flash512 KiB；两个EIDE配置也是512 KiB | 按已确定的ZGT6统一，普通SRAM仍128 KiB |

旧汇总中的“M10P已出局”“先保留LD14P”不作为本次限制；“头尾保护相当于CRC”不采纳：合法距离字段误码可以绕过头尾检查。旧评审里不再存在的目录及行号不能直接用作此次修改定位。

### 2.3 本次基线构建

源码提交：`74d277af5ef6b216270d44d098507d7165178ade`。开始时已有未跟踪 `.claude/settings.local.json`，本次不修改。

采用现有隔离构建脚本，输出至 `tmp/m10p_plan/baseline_build`：57个C/汇编源文件，AC5、C99、O0、MicroLIB、单精度FPU；编译链接成功，**0失败、41条告警，RAM48,616 B、ROM35,836 B**。这是当前LD14P工程的构建结果，不是M10P驱动测试结果；也不是Keil/EIDE三个入口均已测试通过。告警须在实施时区分历史项与新增项，不宣称零告警。

证据：`summary.json`、`build_review.json`、`source_hashes.json`、`baseline.map`，均在上述构建目录。

计算复核及资料SHA256另存于 `tmp/m10p_plan/verification.json`。完成文档后再次对照构建时源文件摘要，固件源文件无变化；新增内容仅为本计划及审查辅助文件。

## 3. 极坐标与车辆坐标：先锁定符号

### 3.1 坐标约定

手册俯视图左侧为0°、上方90°。当图中的0°指向车前，图的90°方向就是车右；不能按通常数学图的“上90°”直接认作车左。

- α：雷达原生角，顺时针增加，0°在车前。
- β：雷达零方向相对车前的安装偏角，顺时针为正；本次名义值为0°。
- θ：旧算法极角，0°右、90°前、180°左、270°后。
- `α_vehicle = wrap360(α + β)`；`θ = wrap360(90 - α_vehicle)`。
- `x = r * sin(α_vehicle)`，`y = r * cos(α_vehicle)`；等价于`x=r*cos(θ), y=r*sin(θ)`。
- 若后续测得雷达相对控制参考点平移(tx,ty)，仅在指定适配出口加到x/y，单位mm。首版算法原点仍取雷达中心；障碍碰撞范围使用相对雷达的真实车体尺寸，不虚构安装偏移。

| 实物方向 | α | θ | r=1000 mm时(x,y) |
|---|---:|---:|---|
| 正前 | 0°/360° | 90° | (0,+1000) |
| 正右 | 90° | 0° | (+1000,0) |
| 正后 | 180° | 270° | (0,-1000) |
| 正左 | 270° | 180° | (-1000,0) |
| 右前 | 45° | 45° | (+707.1,+707.1) |
| 左前 | 315° | 135° | (-707.1,+707.1) |

适配层只转换一次；整圈展开使用原生增加方向，左右提取使用算法角。不能先反转α再在HANDLE2中再反转一次。舵机增加值向左的当前约定保留，并以架空测试确认右侧障碍/左侧偏移的纠偏符号。

### 3.2 包内角度

先遍历70个原始16位距离槽，统计 `m = raw != 0xFFFF` 的数量。m=0仅记录已接收的空扇区，不除法、不输出伪点。

对非FFFF槽按出现顺序分配N=0…m−1：

```text
alpha_cdeg = wrap36000(start_cdeg + round(1500 * N / m))
theta_cdeg = wrap36000(9000 - alpha_cdeg - beta_cdeg)
```

整数运算用足够宽的中间值，明确取整规则；0.01°量化最大约0.005°，不因0.5°索引把真实角提前改成格中心。m包含尚未做物理范围过滤的零距离/过近/高反点；之后距离过滤不重新编号，不重新压缩角度。

例：start=17300、m=70，首点173°、末点约187.7857°；m=69末点约187.7826°。不能用15/(m−1)，否则末点会错误落到下一包起点。跨360°逐点归一化。

协议明确采用15/m，本版照此设计；真实FFFF是否代表补齐槽、是否与本机固件一致，须用部分FFFF的实包和固定目标验证。未核实前不自行改成固定15/70，也不复制例程“起点减10°/减5°”。

## 4. 串口、电源和工程配置

| 项目 | 实施值/要求 |
|---|---|
| USART2 | 512000、8数据位、无校验、1停止位、无流控 |
| RX | PA3/AF7，M10P TX接此引脚 |
| TX | PA2/AF7，需启动指令时接M10P RX；取消M10P模式的TIM9复用 |
| DMA | DMA1 Stream5 Channel4，外设/内存均byte，MINC，Circular，HT/TC与错误处理 |
| 时钟 | 保留当前168 MHz/HSE配置，核验板载晶振及PCLK1实际42 MHz |
| 电机、舵机、编码器 | 保持PA5/PB15、PA6、PD12/13，不能照旧记忆改回PB11/PB10 |
| 蓝牙、调试 | 保留USART6 PC6/7与现有USART3调试，限制阻塞输出 |
| 电源 | 裸机按5 V、4.75～5.25 V、3.3 V TTL、共地；按接口定义接线 |

若PCLK1=42 MHz且OVER8=0，BRR=0x52时实际波特率为42,000,000/82=512,195.12 bps，相对512000约+0.0381%。这是计算值；通过BRR/RCC读回和示波器确认，不改PLL来勉强凑整波特率。

裸机供电能力至少5 W、纹波要求80 mV以内，手册工作电流典型400 mA/最大450 mA；不可把转换盒“3.3～15 V”接到裸机供电。端子顺序按观察方向核验，不能只凭线色。手册未充分给出启动峰值，需实测。

`USER/Template.uvprojx`及两个`.eide/eide.yml`统一F407ZG/1 MiB Flash和对应烧录配置；保持STM32F40_41xxx及现有启动文件。核验实际启用的scatter，不只改Device标签或生成目录的临时sct。DMA对象map地址必须处于0x20000000～0x2001FFFF。

**启动命令仍缺资料：**本机可能上电输出，也可能需要命令，手册ROS章节不能直接证明串口实物的默认状态。台架先验证冷启动；需要命令时取得厂家原始串口字节、应答与时序，加入非阻塞启动状态机和超时计数。不得把ROS命令字符串发送到串口，不猜命令。

## 5. M10P解析器详细契约

### 5.1 字段表（偏移从0开始）

| 偏移 | 长度 | 含义 | 检查/转换 |
|---|---:|---|---|
| 0～1 | 2 | 帧头 | A5 5A |
| 2～3 | 2 | 总长度 | 大端，必须0x00A0=160 |
| 4～5 | 2 | 起始角 | 大端，0～36000；36000归零，其余越界拒绝 |
| 6～7 | 2 | 转速计数speed | 大端；0禁止除法，包不进入有效扫描 |
| 8～147 | 140 | 70个距离槽 | 大端；先FFFF判无效，再提取bit15及低15位 |
| 148～157 | 10 | TIME保留 | 不作为可靠GPS时间，不要求为0 |
| 158～159 | 2 | 帧尾 | FA FB |

`rpm=2500000.0/speed`，`Hz=2500000.0/(60*speed)`，`dps=15000000.0/speed`。中间避免16位乘法溢出，日志保留原始speed和换算值；诊断字段若范围不足，应置无效/饱和标记，不强制截断。转速3472对应约720.046 rpm、12.0008 Hz、4320.28°/s。

`0x9388`→5000 mm且高反；`0x1388`→5000 mm且非高反；`0xFFFF`必须先排除，不能成为32767 mm有效点。高反标志不等于错误，不默认删除铝管回波。M10P没有LD14P强度字段，质量状态须标“不可用”，不伪造置信度数值。

### 5.2 状态机与恢复

1. 搜索A5，再匹配5A；`A5 A5 5A`保留第二个A5。收到第4字节即可拒绝非法长度。
2. 固定160 B候选缓存，跨任意DMA边界保留状态；Feed一次可含0字节、1字节、半包或多包。
3. 完整候选验证长度、头尾、角度、转速，再统一解码；未验完不向扫描器追加部分点。
4. 验证失败从候选的下一字节重搜，保留可能起始头；不得盲跳160 B，也不让伪长度扩大数组或无限等待。
5. 有效包载荷中的A5 5A/FA FB按距离/保留字段处理，不把中途字节识别为包边界。
6. UART/DMA/FIFO错误提升`loss_epoch`；主循环丢半包和当前扫描。队列恢复采用明确水位/代次，不让丢失前后字节直接拼接。
7. 可解析性与可控性分离：合法但转速未稳定、全无回波的包计入健康统计，但不自动授权车轮运行。

结构检查没有CRC等价能力；载荷单比特翻转仍可能得到合理距离。通过电气质量、时序/邻域一致性降低风险，不能宣称检出所有错包。

## 6. 连续DMA、时间戳与调度

### 6.1 接收流程

```text
USART2 -> 1024 B循环DMA -> HT/TC每次512 B
       -> 8192 B软件字节队列 + 块结束时间/字节水位描述符
       -> 主循环M10P解析 -> 单圈扫描 -> 角度索引/感知 -> 控制
```

ISR只处理已完成半块、复制到FIFO、发布索引和记录错误，不打印、不求三角函数、不拟合。正常接收不disable DMA，不重装NDTR。队列用单生产者/单消费者、对齐uint32单调计数，按写数据→内存屏障→发布head顺序；tail只由消费者推进。索引溢出按无符号差处理，负载上限远小于2^31。

FIFO不足容纳整个半块时，整块拒绝并标失流，不部分写入后当完整输入。生产者不得擅自修改消费者tail。消费端以短临界区对齐丢弃水位和loss_epoch，重置解析/组圈，再恢复。描述符满等同失流，不准丢时间信息而保留“新鲜”数据。

时间描述符建议32项、每项16 B，保留累计字节终点、接收时间、代次等。包时间依据所属接收块，附最大块收集延迟的不确定度；**禁止使用主循环解析时刻冒充采集时刻**。包内点时刻只能依据角度/转速估计，GPS保留字段不能补这个缺口。

### 6.2 带宽和时间预算（计算值）

| 项目 | 结果 | 含义 |
|---|---:|---|
| 满线8N1 | 51,200 B/s | 512000/10 |
| 单包线上时间 | 3.125 ms | 160/51200 |
| 24包/圈、12 Hz名义流量 | 46,080 B/s | 约90%线路负载；非实测 |
| 一圈 | 83.33 ms、3840 B、最多1680槽 | 组圈看角度，不按这些计数硬切 |
| DMA半块覆盖时间 | 满线10 ms；名义平均约11.11 ms | DMA复写约束用满线计算；停流时不足半块可无限等待 |
| FIFO容量 | 满线160 ms | 抖动吸收能力，不是允许的控制延迟 |
| DMA中断次数 | 名义约90次/s，满线100次/s | 远少于逐字节接收 |

初版仅HT/TC，停流尾部不足512 B允许不发布完整扫描，超时保护照常触发；不把UART IDLE当一圈结束。若后续加IDLE/定时尾部收取，必须与HT/TC共用唯一消费水位并验证边界去重，不另起第二个FIFO生产者。

最坏DMA服务应显著短于10 ms，初始验收目标ISR执行<100 μs、触发到完成<1 ms；这是工程目标，实测决定是否满足。TIM6当前靠1 ms更新中断维护时间，多次溢出被合并会丢时钟，故单看该时间基准不能证明中断从未迟到：用DWT/GPIO/逻辑分析仪交叉测最大关中断时间和延迟。不能只调NVIC子优先级就当作可以抢占。

主循环按受限批量（初值1024 B）解析，再服务遥测/调参；有积压时优先继续排流。感知仅对最新合格快照执行一次，重复读取同一seq不得再次输出或刷新看门狗。FIFO高水位初设6144 B用于诊断，但数据年龄超限应更早拒绝，不能等溢出才停。

## 7. 完整圈、点云容量和所有权

### 7.1 圈边界

- 原生α=180°对应车后，作为逻辑切点。用连续展开角判断跨过切点，不要求包起始角恰好180°，也不要求起始角是15°整数倍。
- 一个包可以跨切点：按点归属拆分，左闭右开，避免重复或漏点；包覆盖元数据同样拆分。全FFFF包仍贡献15°“已接收覆盖”，不贡献有效回波。
- 刚启动丢弃不完整段，从一个确认边界收至下一个确认边界后才形成完整圈。丢失后重新寻找边界，不能靠凑够1680点伪装恢复。
- 相邻包起始角展开增量名义约15°；初始容差建议±1°，超过后按重复/缺包/乱序/异常分别分类。容差须用原始包验证，不拿转速变化当任意角跳变的理由。
- 记录完整圈时间、包覆盖区间、最大缺角、原始槽/FFFF/物理有效点、高反数、loss_epoch。包数只是旁证，24个重复包不等于一圈。
- 可疑大跳变不更新正常角状态；重同步恢复须连续合理包及重新跨边界。整圈点数>2048、无边界超时、时间倒退或代次变化立即拒绝该圈。

本协议没有包序号，恰好丢掉整圈时角度可能仍“连续”。必须联合接收时间间隔、转速估计及loss_epoch判断；软件的packet_seq只是本机计数，不能证明雷达侧没有丢包。没有足够证据确认连续性时，该圈降为不可用，不声称可精确定位全部丢包。

### 7.2 结构与双缓存

建议每点8 B：`uint16_t angle_cdeg, range_mm, offset_100us; uint8_t quality, flags;`，编译断言sizeof=8；quality对M10P标不可用。0.1 ms相对时间只作接收估计，时间溢出或圈龄过大整圈拒绝。

双缓存各2048点，共32768 B。主循环独占解析和扫描状态：FREE→WRITING→READY→READING→FREE；ISR只生产字节。在算法处理READING期间FIFO暂存新输入，恢复解析后继续写另一份。没有空闲槽时可丢未读READY旧圈并计数，**绝不覆盖READING**。若未来把解析移入其他线程/中断，必须重新设计所有权。

### 7.3 新鲜度

至少记录`scan_start_rx`、`scan_end_rx`、`front_oldest_rx`、`control_apply`；同时报告圈内时间跨度和处理等待时间。以车后切圈虽然避免切开前区，但前区到发布仍存在半圈量级等待；不能把12 Hz误称为83 ms端到端上限。

首轮台架阈值建议：圈周期60～120 ms作为稳定工作窗口、最近前方有效观测最坏年龄150 ms上限、处理耗时目标<20 ms且单次不得长期超过40 ms。阈值不是厂家额定容差，应经实物冷启动和运行分布标定后固定。年龄计算加入块时间不确定度；更高车速须按制动距离收紧。

## 8. RAM预算与效率改进

### 8.1 保守预算

为避免低估，以下**先保留当前全部内存，不扣将删除的3×1798 B旧接收/快照和47 B包缓存**，新增资源再叠加：

| 项目 | 字节 |
|---|---:|
| 当前基线RW+ZI（含现有栈/堆） | 48,616 |
| 循环DMA | 1,024 |
| 软件FIFO | 8,192 |
| 32×16 B接收时间描述符 | 512 |
| M10P候选包 | 160 |
| 2×2048×8 B原始点 | 32,768 |
| 720×2 B代表点索引 | 1,440 |
| 720×4 B有效/计数/近障辅助信息 | 2,880 |
| 元数据、健康计数和新增小状态预留 | 2,048 |
| 增强感知临时工作区上限 | 4,096 |
| 保守合计 | **101,736** |
| 距128 KiB剩余 | **29,336** |

上述是预算而非新固件map，任何新增的点坐标缓存/遥测队列都要记账；4 KiB增强工作区不足时先调整设计，不静默加数组。当前栈4096 B、堆512 B已经计入基线，不重复计算。最终门槛：普通SRAM总占用≤112 KiB、保留至少16 KiB静态余量，并实测栈水位；静态空余不能替代栈溢出检查。

首版不依赖CCM，也不需要外扩RAM。若以后使用CCM，仅放明确CPU独占数据，显式section与链接地址，不能让DMA数组被通配分配进去。

### 8.2 索引和计算量

原始点一次O(N)建0.5°角度格，保存真实点索引、有效性和支撑数。每格近障保留最近合法距离；边界代表点从邻域几何一致的候选选择，不把多层深度简单平均成不存在的墙。

首轮适配可使用最近有效代表点，后续加入异常点/连续段约束；选点规则与相同距离的确定性裁决写入测试。空格保持未知，不插零距离墙、不沿用上一圈有效位。

HANDLE4/6/7及宽度计算从“每个查询角扫全部点”改为索引查询相邻格，查询后仍检查真实角差。0°/360°按循环距离，输出左右边界保持原方向顺序。同一物理点被多个查询角命中时去重或降低独立支撑计数，防止伪造高置信度。

三角函数只对进入工作集/近障评估的点计算并复用，统一float与`arm_sin_f32/arm_cos_f32`，检查隐式double；先实测再决定是否加Flash查表。不在中断中全圈转坐标，不在每包中清800/2048点数组。容器只清计数/有效标记，所有消费者严格遵守实际数量和容量。

## 9. 感知与控制算法升级

### 9.1 随驱动必须完成的兼容性修改

1. `parsed_points`、工作点数、边界点数分别管理，所有写数组接口增加容量；不再传固定800处理有效子集。
2. HANDLE5/5_2的0返回值目前混合了空旷、无点和不可拟合。改成`CLEAR/BLOCKED/UNKNOWN`加独立点数；未知不能解释成通路。
3. “正前方≥5个远点就空旷”的条件对点密度敏感：先按独立角格计数，再检查走廊内近障与覆盖，禁止背景远点压过近障。
4. HANDLE8/9显式要求足够点数，固定6点窗口只能作用于连续、间距受限的角度样本。缺角两侧不能压紧数组后当连续点。
5. 清除各帧拟合有效标记，不把旧k/b、旧宽度当本帧观测。保持值必须带年龄/held状态，过期后不参与授权。
6. 保留现有D尺度及转向限位。首次有效、模式改变、输入seq跳跃、时间异常、感知失效时重建D历史。
7. 遥测`input_seq`从DMA块语义迁移到扫描语义时须版本化；新增`rx_block_seq`、`packet_seq`、`scan_seq`，不能悄悄重解释旧字段。

### 9.2 增强边界与中线（完成接收验收后启用）

- 原始高密度点按角度连续、欧氏邻距和深度跳变分段；异常单点降权，但近距离孤立点在安全通道保留为不确定近障，不因“不像墙”删除。
- 几何邻距基线用`2*r*sin(Δα/2)`，阈值包含测距噪声和几何允许量；不假定任意稀疏点都应相连，也不把相邻点距离阈值与15°包间距混用。
- 左右边界分别拟合，使用有上限的稳健迭代（例如最多两次残差剔除/加权）。纵向边界优先`x=a*y+b`，避免直道竖线在旧`y=k*x+b`下病态；前方横墙保留独立模型，必要时用法向式，不把不同模型的斜率混入旧模式阈值。
- 置信度同时看独立角格数量、连续覆盖、纵向跨度、残差和年龄。高密度点数多本身不证明可用。
- 双边在相同y位置求中点；宽度估计取多位置稳健统计并限速更新。直道宽度只有在双边可信时更新，弯道不强行学习。
- 只有单边时，使用最近可信宽度和边界法向偏置生成参考，降低速度并限制持续时间；不直接把左右距离求和当任意弯道的法向赛道宽度。
- 当前600～900 mm宽度门限与旧资料约1 m赛道可能不符。将最小/最大有效宽度、名义宽度、车宽和余量分开参数化，实测后确定；不直接把默认值拍成1000 mm。

### 9.3 管缝、弯角与S弯

断点候选可用`|r1-r2| > max(J_abs, J_rel*min(r1,r2))`，并要求角跨度、近侧支撑、候选前方距离和相邻段方向一致。J_abs/J_rel是可标定参数，不把400 mm旧常数直接按点率缩放。

管缝后背景点不能仅凭更远就触发大转弯：检查近侧边界是否在短缺口后恢复、对侧边界是否延续、前方是否有横向封闭边界。未知缺口降低置信度，不拟造墙，也不声明可通行。

左右同时出现断点不再总是“只保留较近一个”即可证明转向正确；结合前方自由走廊和两侧连续性。仍然歧义时减速/停车，不能声称已解决岔路任务。

S弯换向需新的相反方向几何证据可中断旧TurnGuard；同一旧圈重复调用不能累计出弯确认。两帧确认保留最小独立观测数，同时加时间条件（建议初值约120 ms，待回放标定），从而明确其12 Hz下行为。350 ms保持上限不当作允许无感知开350 ms。

### 9.4 控制器与速度规划

首版保持当前0～9模式控制器，以便定位雷达链路变化；增强版逐步引入预瞄参考而非同时重写所有模式。使用`L=clamp(L0+k_v*v,L_min,L_max)`选择前视距离，低置信度缩短预瞄并减速；v需先从当前编码器自定义速度标定为mm/s。

误差采用明确的横向偏差及航向项，弯道增加有界前馈；保持舵机最终唯一出口及1170～1720/1445当前限位中位（数值以实物标定为准）。输出限速按PWM/s和实际dt计算，不按每帧固定步长；紧急安全策略优先于平滑。

现有D为`10*kd*(e-e_prev)*115000/dt_us`，参数仍沿用“115 ms参考周期”尺度。改为秒制导数时必须换算参数并更新蓝牙参数版本，不能只换公式。建议对D做时间常数滤波，`a=dt/(tau+dt)`；dt太小/太大或观测失效不放大噪声。

目标速度拆成用户请求速度、感知限速和最终速度。最终取弯曲程度、车体走廊净空、输入年龄、置信度等约束的最小值，并限制加速度；TIM5仍10 ms执行PI，不改成雷达包率控制电机。

制动距离设计采用统一单位：`d_required=v*T_total + v²/(2*a_decel) + margin`。T_total含扫描年龄、调度、执行器延迟；a_decel必须实测。当前零占空比只撤销驱动力，不等于主动制动，不能用它臆算紧急停车距离。未完成轮径/编码器单位和减速标定前，只做保守限速，不启用基于虚假m/s的公式。

### 9.5 故障与恢复

新增状态`WARMUP/READY/DEGRADED/STOP_LATCHED`：

- WARMUP：上电零输出；连续3圈满足时间、覆盖、坐标和感知要求后才允许进入READY（建议初值）。
- READY：只对新seq、未过期且可用感知刷新控制完成状态。
- DEGRADED：单边/局部缺测可按可验证置信度降速；核心前方观测未知、超龄或流错误则TIM5最迟下一10 ms撤销推进许可，不等待旧500 ms兜底。
- STOP_LATCHED：保留现有严重超时锁停，需复位或未来明确的人工重新使能流程；不因重新收到字节自动冲车。

`Radar_ControlCompleted`需带扫描seq和接收时间，或由唯一审核出口调用；处理FIFO旧数据不能不断清龄。保留独立500 ms硬兜底，同时新增基于真实观测年龄的更早许可失效。启动、暂停、恢复时显式清PI积分；当前`PID_realize`内部静态`err_sum`需改成可复位状态，不能只重置pid结构体。

遥测保持HOLD与INVALID区别。候选无效时TurnGuard不得续命；收到了完整但全FFFF的圈必须报告无回波并停/降级，不能因为包数量正常继续开。

## 10. 文件与接口修改清单

以下路径均相对主工程 `2-LXY传承/LXY传承-副本`，新增模块名称实施时可微调，职责不可混淆。

| 文件 | 具体改动 | 验证 |
|---|---|---|
| `HARDWARE/DMA/DMA.c/.h` | 循环DMA、HT/TC、FIFO、错误/水位/时间描述符；删除旧单完成位契约 | 环绕、覆盖、溢出、延迟与中断故障注入 |
| `SYSTEM/usart/usart.c/.h` | M10P 512000及USART2错误中断，SR/DR清错遵循RM0090；避免RXNE与DMA抢读 | BRR、ORE/FE/NE、恢复后重新组圈 |
| 新`HARDWARE/LIDAR/lidar_config.h` | 型号、容量、β、范围/时间阈值、配置版本 | 编译断言、合法范围检查 |
| 新`lidar_protocol.c/.h` | 160 B流式解析、原始状态、高反、角度、转速 | 黄金帧、碎片、错位、模糊输入 |
| 新`lidar_scan.c/.h` | 原生角展开、车后切圈、双缓存所有权、扫描健康 | 半圈、丢包、跨切点、全FFFF、超容量 |
| 新`lidar_adapter.c/.h` | 坐标转换、720格索引、实际计数 | 四象限、偏角、环绕、空格和去重 |
| `HARDWARE/LEIDA_DATA/LEIDA_DATA.c/.h` | HANDLE1改明确适配入口或退出M10P路径；HANDLE3_2/4～10及宽度改按数量/索引/状态 | 直道、竖线、缺角、近障、数组边界 |
| `USER/main.c` | 初始化M10P、删M10P模式TIM9调用、非阻塞调度、扫描授权及单次输出 | 无数据、旧数据、重启、调参同时运行 |
| `HARDWARE/CENTRE_LINE/CENTRE_LINE.c/.h` | 保留PD已有修复；增强模型与置信度、TurnGuard时间契约、PI复位 | 左右镜像、模式切换、S弯、dt变化 |
| `HARDWARE/LEIDA_TIMER/timer.c/.h` | TIM5基于感知许可/年龄仲裁速度；锁停保持 | 主循环停滞、传感器断线、过期输入 |
| `HARDWARE/TIM/PWM/PWM.c` | 雷达PWM只在LD14P路径被使用，核验PA2 AF；不改车轮/舵机映射 | 寄存器和实际波形 |
| `HARDWARE/hc-05/ble_diag*、ble_tune*、ble_tx*` | M10P统计、参数/协议版本、输入来源时间、配置快照 | 现有V2黄金向量和新消息兼容 |
| 现有PC采集/解码/报告工具 | 区分LD14P CRC与M10P无CRC、扫描与DMA块、真实时间和新字段 | 新旧日志可识别，不悄悄错解 |
| `USER/Template.uvprojx`、两份`.eide/eide.yml`与实际链接设置 | 新模块加入三个入口、ZG配置、Flash/烧录算法一致 | 三入口文件清单/宏/布局对照，实际构建 |
| `test/` | 增加M10P解析/扫描/感知/控制回归，保留已有雷达与转向用例 | 生产C实际运行和目标编译分别报告 |

建议接口语义：

```c
void LidarRx_Init(void);
size_t LidarRx_Read(uint8_t *dst, size_t capacity, RxStamp *stamp);
void M10P_Feed(const uint8_t *data, size_t count, const RxStamp *stamp);
bool LidarScan_Acquire(const LidarScan **scan);
void LidarScan_Release(const LidarScan *scan);
bool LidarAdapter_Build(const LidarScan *scan, AlgorithmInput *out);
void Control_Submit(const AlgorithmInput *in, uint32_t now_us);
```

Read返回连续时间/代次段，不跨元数据边界混成同一时间；解析通过包事件交给扫描器。上述为设计接口，实施时同时给出结构体、容量、失败返回与线程所有权说明，不只添加几个声明。

## 11. 遥测、可观测性与回放

必须同时具备：字节数、HT/TC数、队列峰值、UART各错误、DMA错误、FIFO/时间描述符溢出、失流代次、包合法/长度/尾/角度错误、FFFF/高反计数、包间最大缺角、圈序号、圈周期/覆盖/有效点、原始与工作点数、控制采用的scan_seq、各阶段耗时、前区年龄、最终舵机CCR与最终电机许可/目标速度。

不能把M10P的“CRC错误=0”显示成校验通过；标注该传感器无CRC。新增消息优先走V2扩展类型；若变更旧字段语义/布局，提升版本并同步PC侧测试。模式和新参数写配置快照，便于区分驱动版与增强算法版。

115200蓝牙不能全量承载约46 KB/s雷达流；只传低频健康、控制特征和限量点云。原始包通过支持512000的有线采集/逻辑分析仪记录，RX旁路不并联两个TX。旁路记录仍需确认其自身不丢字节。

每次运行保存设备/固件/接线版本、源代码摘要、参数、原始串口流与时间、遥测、错误事件和结论。不得用仅有人工合成帧的数据宣布真实FFFF或冷启动已验证。

## 12. 验收矩阵

### 12.1 解析与坐标离线测试

| 测试组 | 必须覆盖 | 通过条件 |
|---|---|---|
| 标准包 | 全70点、长度160、3472转速、173°起角 | 点数、距离、角度、转速符合独立参考解码 |
| FFFF | 首/中/尾1个、多个、69个、70个 | m/N正确、m=0不除法、无伪障碍 |
| 高反与物理过滤 | 0x1388、0x9388、0xFFFF、0x8000、0、过近/过远 | 先FFFF再掩码，过滤后不重排角度 |
| 任意分块 | 160 B每一个切分位置、逐字节、多包连喂、DMA边界 | 输出与整流喂入一致 |
| 错流 | 插/丢字节、A5 A5 5A、载荷含头尾、假Len=20/255/65535、错尾 | 不越界/死循环；后续完整好包可恢复，失流圈不授权 |
| 角度/速度 | 0、35999、36000、36001；speed=0/极端值 | 范围明确、归一化正确、无除零/窄化回绕 |
| 坐标 | 前后左右、45°/315°、非零β、角度回绕 | 符号与第3节表一致，误差符合量化 |
| 无CRC边界 | 只改合法距离载荷 | 承认部分错误无法结构检出，不伪报完整性保证 |

测试必须调用实际生产解析C，不只测试Python复制逻辑。主机C运行与ARM编译链接分开记录；只有生成axf不等于测试已运行。加入有限长度随机输入/容量哨兵，检验越界和前进性。

### 12.2 扫描、接收与控制回归

- 任意起角启动、跨车后切点的包、全空扇区、重复/乱序/丢一个包、丢一整圈、圈点数溢出、慢速启动、时间计数回绕。
- 只有包覆盖但无回波时禁止授权；重复seq、陈旧FIFO数据、READING缓存被写均应被测试捕获。
- FIFO整块满、描述符满、HT/TC同时挂起、服务过迟、UART ORE、DMA TE/DME，错误后重新建立完整圈。
- 直道居中/左右偏、单侧墙、两侧断点、90°弯、S弯换向、不同宽度、管缝后背景、前方小障碍、无回波与稀疏回波。
- 同一场景左右镜像得到镜像控制方向；模式转换无反打，TurnGuard无无限续期，PD不重复dt缩放。
- 主循环不再处理有效观测、雷达拔掉、全FFFF输入、停流不足半块、遥测拥塞，TIM5仍能撤销推进。
- PI暂停/恢复清状态、目标限速不被蓝牙直接写入绕过、无授权时初值和最终CCR均为零推进。

### 12.3 台架与实车门槛

1. 上电前核对5 V供电、TTL电平、PA2无雷达PWM、芯片工程配置；先架空车轮。
2. 冷启动至少10次，记录从上电到稳定点云的时间和是否需命令；验证0°前、90°右、270°左的固定挡板。
3. 捕获全有效/部分FFFF/连续FFFF原始包，与固定墙和厂家显示对比；实测bit15含义及角度间距。
4. 满线路速率回放至少10分钟，同时开启蓝牙遥测/改参及10 ms速度环；目标UART/DMA/FIFO错误为0，出现一次也须解释定位。统计峰值，不只看平均值。
5. 测DMA响应、最大关中断、FIFO高水位、处理P50/P99/最大、圈龄及前区年龄；验证TIM6时钟可靠。新map及栈水位满足第8节门槛。
6. 近场铝管/白黑目标50/100/200/500/1000/2000 mm、多角度/光照/遮挡测试；这些是测试距离，不代表厂家保证的最小量程。记录距离偏差、有效率、高反比例和抖动。
7. 架空验证失流、无回波、主循环阻塞、锁停和复位恢复；随后低速直道，再左右弯、S弯、缝隙和障碍。安装高度/俯仰须能持续扫到管体。
8. 分别比较“新驱动+兼容算法”和“新驱动+增强算法”，同赛道同速度多次复跑；记录碰撞/脱轨、漏弯、假断点、摆动、输入年龄和完成时间。未通过不提速。

编译正确不能保证“一次上车成功”。本计划的目的在于提前固定协议与坐标、暴露资料缺口，并用可重复门槛尽量减少返工；最终成功必须以实机证据认定。

## 13. 实施顺序、交付与回退

| 阶段 | 修改范围 | 完成门槛/交付 |
|---|---|---|
| P0 基线固化 | 本轮已完成资料/源码核对与隔离构建 | 本文、基线map/摘要；待敲定方案 |
| P1 工程与接收 | ZG配置、PA2处理、512000、循环DMA/FIFO/错误恢复 | 寄存器核对、连续流压力测试，电机不授权 |
| P2 协议与扫描 | M10P解析、坐标、完整圈、双缓存、时间/容量检查 | 生产C离线测试、实包对照、静态点云正确 |
| P3 兼容车控 | 720格适配、真实数量、感知状态、输入年龄、PI复位、遥测 | 现有回归无退化，失效停机通过，低速可运行 |
| P4 增强感知控制 | 稳健边界、缺口/近障、时间确认、限速/预瞄 | 离线场景矩阵、影子输出对照，再低速启用 |
| P5 验收交付 | 三构建入口、CPU/RAM/时序、实车日志和参数 | 固件及源码摘要、测试报告、接线/操作说明、回退配置 |

每阶段保留可构建快照和配置版本。保留现有LD14P代码作为历史基线或显式编译选项，不在运行中靠头部猜型号。回退LD14P固件须同时换回对应雷达和接线，不能把旧固件烧在已接M10P上就称完成回退。

P4可先“影子计算”只记录建议、不驱动，比较与P3的差异；测试通过后再启用。任何阶段失败，回退到已验收的硬件/软件组合，不覆盖原版和谢露工程。

## 14. 敲定项与实机待补信息

建议整体接受第1节方案，并按P1→P5实施。无需再选择主控型号或雷达前方；本次已经明确。

以下信息不阻碍本计划产出，但分别是后续通电/参数启用/实车放行的条件：

| 项目 | 当前状态 | 最晚解决阶段 |
|---|---|---|
| 使用本仓库“LXY传承-副本”为后续修改入口 | 按当前活跃代码与旧评审推定 | 开始固件实施前敲定；若实际烧录目录不同，只改变交付入口 |
| M10P串口实物型号/固件及6P观察方向 | 文档匹配，未见实物接线 | P1通电前 |
| 上电自动输出还是需启动命令 | 资料未给原始命令 | P2台架 |
| 部分FFFF的实际角度语义 | 按协议15/m设计，未抓实包 | P2放行前 |
| 板载HSE、实际USART2引脚连接 | 源码8 MHz/PA3，待板上核验 | P1 |
| 安装偏角、相对车体位置、车宽/前悬 | 名义β=0；平移和车体尺寸未测 | P3安全走廊启用前 |
| 赛道有效宽度、轮径/速度单位、减速能力 | 不采信未标定常数 | P3低速限值/P4动态速度规划前 |
| 舵机中位、限位、左右方向 | 沿用当前1445、1170～1720 | P3架空验证 |

本轮没有实施上述固件改动；等待对这份具体计划敲定后进入P1。手册缺失的启动报文与实机性能将明确标记未验证，不以推测填补。
