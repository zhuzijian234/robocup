# 雷达车工程 — 对话记忆点（跨对话交接用）

> 生成日期: 2026-08-29。用途: 换新对话时把本文件内容贴给 Claude，避免重新推导。

## 0. 背景一句话

让 LXY 的雷达车代码（STM32F407VET6 @168MHz，标准外设库）在**谢露的车上**跑通，并新增**蓝牙 VOFA+ 实时调参**功能。用户是 C 语言初学者，用提问式学习（指针、atoi、字符串解析），偏好内存布局图 + 步骤表格 + 具体数值的中文讲解，一次一个问题。

## 1. 文件位置（重要！存在多份副本）

| 路径 | 用途 |
|---|---|
| `d:\OneDrive\Desktop\传承\2-LXY传承\LXY传承-副本\` | 用户编译烧录的主用工程（**已失去 .git**） |
| `d:\OneDrive\Desktop\robocup\2-LXY传承\LXY传承-副本\` | 用户当前的 **git 仓库副本**（origin = github.com/zhuzijian234/robocup.git） |
| `d:\OneDrive\Desktop\传承\谢露代码+LD14P产品手册\谢露代码\` | 谢露的旧代码（只读参考，本地仓库已提交未推送） |
| `d:\OneDrive\Desktop\robocup\谢露\` | 谢露代码的另一份拷贝 |
| `LXY传承-副本\蓝牙实时调参方案.md` | 蓝牙调参方案文档（已多轮评审修订；**用户会手动改它，编辑前必须先重读**） |

⚠️ 两份 main.c 曾完全一致，2026-08-29 已把 break 修复同步到两份。以后改代码先问清改哪份（建议以 Desktop\robocup 为唯一真身）。

## 2. 硬件与引脚

- STM32F407VET6 @168MHz，标准外设库，**MicroLIB**（eide.yml `use-microLIB: true` → 没有 sscanf %f / strtof，调参方案用整数缩放+atoi 规避）
- USART1 PA9/PA10 @115200 = printf 调试串口；USART2 PA3/DMA @230400 = 雷达；USART6 PC6/PC7 @9600 = HC-05 蓝牙（RXNE+IDLE 中断，NVIC (1,1)）
- 舵机 TIM3 CH1 PA6（50Hz：84MHz/(84×20000)）；电机 TIM2 CH4 PB11（20kHz PWM）+ PB10 方向（单IO，高=正转）；编码器 TIM4 正交 PD12/PD13（PPR=11，减速比 6.25，4倍频）；雷达电机 TIM9 CH1 PA2（与 USART2_TX 共用，初始化顺序：TIM9 后）
- 速度环：TIM5 10ms 中断，PI(8.5, 0.505, 0)，Speed_mubiao=17，Speed_now 范围 0~27（拟合单位，moto.c:96 公式 `Encoder_cnt*100/(4*11*6.25)`）
- 谢露版蓝牙从 USART3 移到 USART6，代码注释已适配

## 3. 已确认的关键结论（勿重新推导）

1. **舵机方向约定两车一致，不反向**：数值大=左转。LXY：1360=右打满/1800=左打满（main.c:423-428）；谢露：`servo_right=1170`/`servo_left=1720`（她的 Servo.h:5-6）。两边都是 `pwm = mid + kp*err`，err 为正→左转。她的车用她代码跑过 → 物理安装也是"数值大=左"。
2. **舵机中位不同**：LXY=1565（main.c:97，/10 表示 156.5），谢露=1445。差 120 → LXY 代码在谢露车上"回正"实际偏左几度。
3. **舵机行程不同**：LXY 限幅 [1360,1800]（CENTRE_LINE.c:339-340）超出谢露 [1170,1720] 左端 → 打满左会顶机械限位（憋舵机）。
4. **电机驱动接口两车完全相同**：PB10 单IO方向 + PB11 PWM + 编码器 PPR11/6.25 都一样（谢露 moto.c 与 LXY moto.c 一致）。需实车验证：方向是否反、编码器 A/B 相序、20kHz 容忍度、3.3V 逻辑电平。
5. **蓝牙 IDLE 分帧不可靠**（空口分包会把命令切成两半）→ 用 `\n` 内容分帧 + 静态 tail 缓冲跨帧拼接。
6. **VOFA+**：控件（滑块）绑定"连接"而非引擎；RawData 引擎看文本；FireWater/JustFloat 二进制帧尾 `00 00 80 7F` 画波形；滑块默认发 `kp:45`（冒号），所以解析要同时认空格和冒号，**取更靠前的那个作切点**（名字侧要精确匹配 strcmp，值侧有跳过非数字前缀的容错）。
7. **调增益绝不能用 Midline_PD_Init/Speed_PID_Init**（会清零 err/err_l 状态）→ 直接改结构体字段，下一控制周期（~78ms）生效。
8. atoi 的坑：atoi(":493")=0，0 又在合法范围内 → 滑块会静默写 0。方案里已用"跳过非数字前缀 + 全非数字报 ERR fmt"防住。

## 4. 已发现的问题与状态

| 问题 | 状态 |
|---|---|
| **break bug** main.c:219：valid_couter≤20 时 break 跳出 while(1) 主循环 → 整个程序死亡 | ✅ **已修复（两份代码）**：改 continue + 调试打印 `Data invalid, skip frame: %d`（稳定后可删）。**尚未 commit/push** |
| **HW_TEST_MOTOR_SERVO=1** main.c:65：测试模式，舵机[1360,1800]往复扫 + 电机10~100往复 | ❌ 未改。"上电舵机一直左右摇摆"的头号原因 → 建议改 0 |
| servo_pwm 未初始化（main.c:96），`|k|<=0.1` 分支（main.c:450）第一帧可能输出垃圾 PWM | ❌ 未改（瞬态，影响小） |
| 电机初始占空比 90%（main.c:128）→ 上电轮子狂转 | ❌ 未改，测试时把车架起来 |
| 中位/限幅/打满值按谢露车适配：156.5→144.5；[1360,1800]→[1170,1720]；1360/1800→1170/1720 | ❌ 已建议，等用户确认（建议先实车验证 1445 中位） |
| CENTRE_LINE.c:302-336 每帧大量 printf（ERROR/flag/speed/PWM_Before） | 已知，暂不处理 |
| `get` 命令会被 `\r\n` 破坏（strcmp 不匹配） | 已有修复方案（切分时吃掉 `\r`），用户未采纳 |
| 蓝牙方案文档 kp 冒号格式问题 | ✅ 已修入文档（双分隔符方案） |

## 5. 蓝牙实时调参方案状态

- 文档 `蓝牙实时调参方案.md` 已完成多轮评审和修正（2 个编译 bug、1 个功能 bug、若干文档问题都已改）。
- **代码尚未开工**。等用户说"开工"再实施：
  1. 新建 `HARDWARE/hc-05/ble_tune.h` + `ble_tune.c`（按文档 §4.2）
  2. main.c 三处插入：include（53行后）、`BLE_Tune_Process();`（while(1) 顶部 164 行）、`BLE_Tune_Telemetry(Servo_pd.err, servo_pwm, pid_select);`（474 行后）
  3. bsp_bluetooth.h 加一行 `void BLE_Send_Bit(unsigned char ch);` 声明
  4. 构建配置 3 处：Template.uvprojx（hc-05 组 FileType 1/5）、两个 .eide/eide.yml（sources + include）
- 方案核心：参数表整数缩放（mul[4]={1,10,100,1000}）；Tune_ApplyOne 双分隔符取靠前 + 跳过非数字前缀 + 全非数字→ERR fmt；BLE_Tune_Process 静态 tail 缓冲（BLERX_LEN_MAX=200，ISR 封顶 199 保证 '\0' 安全）；遥测二进制用 BLE_Send_Bit（BLE_send_String 发不了 \0），未连接时 `Get_Bluetooth_ConnectFlag()==0` 直接 return。

## 6. Git 状态

- **Desktop\robocup** = 活跃仓库，origin = github.com/zhuzijian234/robocup.git，历史含 "Add files via upload"（用户之前用 GitHub 网页上传过）。
- 传承\2-LXY传承 已无 .git（用户迁移时删了）。
- `传承\谢露代码+LD14P产品手册\` 是我为上传准备的本地仓库（146 文件 20.5MB 已提交、未推送）——用户后来自己用网页上传完成了，此文件夹可能可删（待用户确认）。
- break 修复只改了本地文件，未 commit/push。
- 用户电脑无 gh CLI；推送需用户在网页建库或本地 git + 浏览器登录。

## 7. 待办 / 待用户确认清单

1. HW_TEST_MOTOR_SERVO 改 0 后实车验证（串口判断：刷 `Servo_Test PWM` = 测试模式；只刷 `DMA_RX_DONE` = 数据帧被判无效）
2. 舵机中位/限幅/打满三处改谢露值（先实车验证 1445 是否真中位、方向是否一致）
3. 蓝牙调参方案"开工"（代码实施）
4. robocup 仓库 commit + push break 修复
5. 电机方向、编码器相序实车验证（架起车轮：轮子正转、手拨轮子 Speed_now 为正）
6. 确认 `谢露代码+LD14P产品手册` 文件夹是否删除
7. 可选：`\r` 容错、worked example 入文档（已提未采纳）

## 8. 沟通注意事项

- 用户会在我编辑文档的同时手动改文档 → 每次 Edit 前重读目标文本
- 用户喜欢被问"要不要我把 X 改了"再动手（代码修改必须等确认）
- 已讲过的概念（避免重复长篇）：atoi 原理、指针比较 `sc<sp` 是地址比较=字符串中更靠前、break vs continue 区别、中位偏差→跑偏 vs 方向反→摇摆的症状区分
