# 谢露 vs LXY — 规划与控制算法完整对比

> 生成: 2026-09-06。适用前提:**当前车永远是谢露的车**(舵机 1445/1170/1720、谢露的机械与雷达安装),一切标定与调参以谢露车为准。
> 配套文档: [谢露vsLXY-外设对比.md](谢露vsLXY-外设对比.md)（引脚/外设层面,本文档只讲**规划与算法**层面）。

---

## 〇、一句话结论

两套规划是**完全不同的思路**,不是谁抄谁的关系:

- **谢露**:误差 = 拟合角度 + 归一化偏差,天然有界,**不用斜率做除法** → 没有 k 爆炸/方向翻转整类 bug;但速度开环、蓝牙缺失、代码是测试版
- **LXY**:误差 = 中线 x 偏差(mm),经 **(y−b)/k 反解** → 正是乱摆/错方向的根源(已在 LXY-副本 打补丁);但速度闭环、蓝牙调参、多模式、工程化完整

**最优路线 = LXY 的骨架 + 谢露的信号构造与参数手感。**

---

## 一、控制流程对比

### 谢露(Test/Test.c Handle3_TEST,286 行)

```
雷达帧 → 360°排序 → 后轮区滤波
  → 前方0~180°扫描, 得到 left_duan_y / right_duan_y (两侧墙断点距离)
  → 三分支:
     ① 双侧断点都近(<zhuandian=200)  → 分叉/十字: 亮灯, 跳过(或岔路动作)
     ② 双侧断点都远(无断点)          → 拟合两侧墙角度 K_ave → 取平均 nihejiao_M
                                        - 65°~90°      → R_CL 右曲线
                                        - <65°         → 直接打满右(不走PD)
                                        - 90°~115°     → L_CL 左曲线
                                        - >115°        → 直接打满左
     ③ 单侧断点(弯道入口)            → 只拟合远侧墙 + 三段角度梯度(Tidu)
                                        - 右墙断 → YZ_j 右直角, 左墙断 → ZZ_j 左直角
  → qianfang30_PD(paodao_piancha_G1, nihejiao_G1, ZW_ave_G1, flag)
```

### LXY(LXY传承-副本 USER/main.c,~470 行)

```
雷达帧 → HANDLE1~11 数据提取(中线/左右边界/前方扫描)
  → HANDLE8/9 断点检测 → LEFT/RIGHT_duandian
  → 三分支:
     ① 右断点 → 按前方拟合斜率 |k| 分级: <0.35 大右转(3) / 0.35~0.7+S弯特征 中右转(8) / else 小右转(1)
     ② 左断点 → 对称: 4 / 9 / 2
     ③ 无断点 → 中线拟合 → 垂线判定(5) / 均值兜底(7) / 普通(0)
  → Midline_PD(中线/边界点数组, 10种模式)
```

**关键差异**:谢露用**角度分级**(65°/115° 直接打满的兜底),LXY 用**斜率分级**(|k|<0.35 等)。角度有界(0~180°),斜率无界(竖直墙→∞/0)。

---

## 二、误差构造对比(核心差异)

| | 谢露 qianfang30_PD | LXY Midline_PD |
|---|---|---|
| 输入信号 | 拟合角度 nihejiao_G1(归一化) + 跑道偏差 paodao_piancha_G1(±1) | 中线 x 偏差(mm) |
| 反解方式 | **无除法**,角度直接由 atan 得到 | **(y−b)/k、Δy/k 反解 x** |
| 误差公式 | err = (a·角度 + d·偏差[+ZW·S弯]) × 舵机半行程275 | err = −(x拟合 − 50) |
| 限幅 | ±275(=半行程,有物理意义) | ±500(直道±200,任意值) |
| 极陡弯 | 直接 Servo_ChangePwm 打满,不走 PD | 无兜底,靠限幅后的小增益 |

**为什么这是根本差异**:

1. 谢露的输入全有界 → err 永远有限、连续;**LXY 的 1/k 在竖直墙(k→0)时爆炸、符号随噪声翻转** → 2026-09-06 一天修了两轮(模式3/4/8/9 的 k=0 除零、模式1/2 的 |k|<0.3 符号翻转),都是给 1/k 反解擦屁股。谢露的结构**天生不需要这些保护**。
2. 谢露的 err 按**舵机半行程归一化**(±275),kp=0.5~0.9 直接就是"打多少比例的方向盘",调参直观;LXY 的 ±500mm 与舵量之间没有直觉联系,kp=0.035 这种"毫米单位专用"小增益纯靠试。
3. 谢露的偏差归一化 paodao_G1 还带哨兵值(一侧无墙 → ±2),自动给转弯方向加偏置;LXY 的模式 1/2 补偿项(BLUE_DIS)是硬编码的,方向不完全受控。

---

## 三、模式体系对比

| | 谢露 | LXY |
|---|---|---|
| 模式数 | **4**(R_CL右曲线 / L_CL左曲线 / YZ_j右直角 / ZZ_j左直角) | **10**(0直道/1小右/2小左/3大右/4大左/5垂线/6,7均值/8中右/9中左) |
| 直道处理 | **没有专属模式**——两面墙角度≈90°时误差自然趋近0,直道是曲线的特例 | 专属模式 0/5(依赖标定值 50) |
| 模式切换 | 角度连续变化,弯→直**无跳变** | 离散切换,err 公式整个换掉,**切换瞬间 err 跳变**(D 项还会放大一次) |
| S弯 | ZW_ave 特征项并入误差 | 独立模式 8/9 |
| 极陡弯 | 直接打满(绕过 PD) | 无(只能靠 err 限幅) |

谢露"直道=曲线特例"的设计很优雅:一个连续的角度信号天然覆盖直道/微弯/大弯,不存在 LXY 那种 0↔5、1/2↔0 切换的 err 突变。LXY 多出的模式(垂线/均值/S弯)是对抗"1/k 不可靠"和"中线点太少"的**补丁式**设计——换成角度法后大部分都不再需要。

---

## 四、PD 参数与等效换算

### 谢露原值(LEIDA_DATA.c:644-651)

| 模式 | kp | kd | ki | err 权重 a(角度)/d(偏差)/ZW |
|---|---|---|---|---|
| R_CL / L_CL 曲线 | 0.5 | 0.3 | 0 | 0.6 / 0.1 / — |
| YZ_j / ZZ_j 直角 | 0.9 | 0.13 | 0 | 0.6 / 0.6 / 0.12 |

(err ∈ ±275; 曲线模式最大舵量 0.5×275=137.5 计数=50%行程,直角 0.9×275=247.5=90%)

### LXY-副本 main.c 两套参数

| | kp(直道) | kp_2(大弯) | kp_3(小弯) | kd | kd_2 | kd_3 |
|---|---|---|---|---|---|---|
| 第127行(写了但被覆盖) | 0.0575 | **0.14** | **0.0597** | 0.12 | 0.02 | **0.105** |
| 第157行(**现役**) | 0.035 | 0.040 | 0.0395 | 0.075 | 0.022 | 0.020 |

### 等效换算(LXY 单位:舵量 = kp×err×10,err∈±500)

| 谢露手感 | 折算成 LXY 参数 |
|---|---|
| 直角 kp=0.9 → 最大 247.5 计数 | kp_2 ≈ 0.0495(现役 0.040 **接近**,不算短板) |
| 直角 kd=0.13 → 瞬态冲击 | kd_2 ≈ 0.07(现役 0.022,**弱3倍**) |
| 曲线 kd=0.3 | kd_3 ≈ 0.16(现役 0.020,**弱8倍**) |

**结论:LXY 现役"力度不够"的真凶不是 kp,是 D 项(kd_2/kd_3)比谢露弱 3~8 倍 + kp_2 满幅也够不到满舵(0.040×500×10=200 计数,离满舵 275 差 75)。** 第127行那套被覆盖的参数(尤其 kp_2=0.14、kd_3=0.105)恰恰是"谢露手感"的量级。

---

## 五、速度控制对比

| | 谢露 | LXY-副本 |
|---|---|---|
| 方式 | **开环**:TIM5 中断里固定 `Moto_pwm=3300`(占空比 78.6%),蓝牙 stop 才清零 | **闭环 PI**:kp=8.5, ki=0.505, err_sum 抗饱和 ±200,每 10ms 一次 |
| 优点 | 简单,永不振荡,无调参负担 | 上坡自动加力、电池电压不影响车速、可设定目标速度(当前 8) |
| 缺点 | 电池电压/负载变化 → 车速漂移;无制动能力 | 参数要调(已在实车调通) |

**速度环是 LXY 多出的、明显更好的东西,保留。** 谢露车当年开环也跑完过,说明速度精度本身不是瓶颈。

---

## 六、周边配套对比

| | 谢露 | LXY-副本 |
|---|---|---|
| 蓝牙调参 | ❌ main.c 注释掉,USART6 未用 | ✅ 完整(16 条命令 + 7 通道 VOFA+ 遥测,`kp`/`kp2`/`kp3`/`spd` 可现场调) |
| 看门狗 | ✅ IWDG 1s(主循环喂狗,卡死自动复位) | ❌ 无(主循环卡死 → TIM5 中断还继续输出油门,危险) |
| 代码结构 | 测试版:Test.c 里 `#if 1`、continue 后死代码(chalu 不可达)、一半路径未用 | 模块化完整:独立 CENTRE_LINE 模块 + 三份配套文档 |
| 调参手段 | usart.c 里串口命令改 zhuandian/Duandian_d | 蓝牙全参数可调 + 波形回传 |
| 关键常量(谢露车实测) | CL_jixianjiao=65°, zhuan_a1_L/R=120°/60°, chekuan=140, zhuandian=200, Duandian_d=700 | 标定值 50(疑似 LXY 车标定,谢露车未实测!) |

---

## 七、优劣清单

### 谢露的优点
1. **信号构造稳健**:角度 + 归一化偏差,有界连续,无 1/k 类 bug,无模式切换跳变
2. **err 按舵机半行程归一化**:kp/kd 有物理直觉(0.5 = 打一半),调参容易
3. **强微分项**:kd/kp = 0.6(曲线)~0.14(直角),入弯冲击力足
4. **极陡弯兜底**:直接打满,不做无谓的 PID 计算
5. 有看门狗

### 谢露的缺点
1. 速度开环(3300 固定),电池电压影响车速,不能控速/刹车
2. 蓝牙功能闲置(她当年没做完),现场调参要改代码重烧
3. 代码是测试版:死代码、未完成分支(chalu 不可达)
4. 无中线模式/垂线模式——全靠两面墙角度,单侧墙缺失时(弯道)只能靠远侧墙,容错弱
5. 常量是"她的赛道"标定的(chekuan=140、65° 极限角等),换赛道要重测

### LXY 的优点
1. 速度闭环 PI(已调通),上坡/电池/负载自适应
2. 蓝牙调参 + 波形遥测,现场调参零重烧
3. 10 模式覆盖面广(S弯/垂线/均值兜底)
4. 工程化:模块独立、文档齐全、可维护性好

### LXY 的缺点
1. **1/k 反解是乱摆/错方向的根源**(已打两轮补丁:模式3/4/8/9 k=0 除零、模式1/2 |k|<0.3 翻转,仍未根治)
2. err 单位(mm±500)与舵量无直觉联系,增益纯靠试
3. 现役 D 项太弱(比谢露弱 3~8 倍),入弯软
4. 标定值 50 是 LXY 车标定的,谢露车未实测(模式 0/5 有偏置风险)
5. 模式切换 err 跳变(公式整个换掉)
6. 无看门狗;主循环卡死油门会保持

---

## 八、结论与建议(按优先级)

1. **保留 LXY 骨架**:速度闭环、蓝牙调参、模块结构——这三样谢露没有且明显更好
2. **参数换成"谢露手感"**:第157行现役参数 → 第127行那套(kp 0.0575 / kp_2 0.14 / kp_3 0.0597 / kd 0.12 / kd_2 0.02 / kd_3 0.105),或先用蓝牙 `kp2 140`、`kd3 105` 现场验证
3. **根治方向问题(重构级,前两步满意可不做)**:把 LXY 转弯模式的 err 改成谢露的"拟合角度 + 归一化偏差"构造——彻底消灭 1/k 反解及其全部补丁
4. **补看门狗**:IWDG 1s,主循环喂狗——防主循环卡死后油门失控
5. **实测标定值**:按 debug.md 的零代码方法,量出谢露车的"摆正时中线 x",替换 CENTRE_LINE.c 三处 50
